[pytest]
# 测试文件发现
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# asyncio 模式：auto 表示自动识别 async 测试函数
asyncio_mode = auto

# 标记定义
markers =
    integration: 集成测试（需要真实 PG）
    unit: 单元测试（无外部依赖）

# 默认参数
addopts = -v --tb=short
